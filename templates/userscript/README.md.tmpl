# {{NAME}}

{{NAME_EN}}

## Features

{{DESCRIPTION}}

The script runs only on these pages:

```text
{{MATCH_PATTERNS}}
```

## Installation

1. Install a userscript manager such as Tampermonkey or Violentmonkey.
2. Install this script from GreasyFork.
3. Open one of the matched pages above.

## Usage

TODO(userscript): Replace this paragraph with the exact entry point, interactions, and failure states.

## Privacy and network requests

The initial template does not read or upload personal information and makes no network requests. If the implementation adds storage or requests, update this section and the `@grant` and `@connect` metadata before release.

## Compatibility and limitations

The script may need an update when the target site changes. Before release, complete a manual smoke test in every userscript manager the README claims to support.
